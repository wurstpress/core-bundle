{% extends 'WurstpressCoreBundle::layout.html.twig' %}

{% block body -%}

    <div class="span10">

        <h1>{{ 'post.create.label'|trans({}, 'WurstpressCoreBundle') }}</h1>

        <form action="{{ path('post_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
            {{ form_widget(form) }}
            <p>
                <button type="submit" class="btn btn-primary">{{ 'post.create.label'|trans({}, 'WurstpressCoreBundle') }}</button>
            </p>
        </form>

        <ul class="inline">
            <li>
                <a href="{{ path('post') }}" class="btn">{{ 'post.back_to_list.label'|trans({}, 'WurstpressCoreBundle') }}</a>
            </li>
        </ul>
    </div>
{% endblock %}
